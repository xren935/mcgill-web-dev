printf("curr ps is: %s", currpswrd);
                // printf("%d", strcmp(processed, values[i].password));
                // printf("<h1>username should be: %s</h1>", values[i].username);
                // printf("<h1>pswrd should be: %s</h1>", values[i].password);
                // printf("<h1>Wrong username or password</h1>");